
/**
 * Write a description of interface PositionInf here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public interface iPosition
{
    public int getCol();
    public int getRow();
    public boolean equals(iPosition pos);
}
